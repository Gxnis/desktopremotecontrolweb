{
  "name": "remote-desktop",
  "version": "1.0.0",
  "description": "Remote desktop screen sharing with room-based access",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "build": "cd client && npm install && npm run build",
    "start": "node server.js",
    "postinstall": "cd client && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "uuid": "^9.0.0"
  }
}
